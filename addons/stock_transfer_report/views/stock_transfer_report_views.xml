<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- FORM VIEW -->
    <record id="view_stock_transfer_report_form" model="ir.ui.view">
        <field name="name">stock.transfer.report.form</field>
        <field name="model">stock.transfer.report</field>
        <field name="arch" type="xml">
            <form string="Stock Transfer Report">
                <sheet>
                    <!-- Header Fields -->
                    <group>
                        <field name="store"/>
                        <field name="company"/>
                    </group>

                    <!-- Fetch Button -->
                    <group>
                        <button name="action_fetch_transactions"
                                string="🔄 Fetch All Transactions"
                                type="object"
                                class="btn-primary"
                                icon="fa-refresh"/>
                    </group>

                    <!-- Notebook Pages -->
                    <notebook>
                        <!-- Outgoing Lines Page -->
                        <page string="📤 Outgoing Lines" style="background-color:#FEF9E7; padding:5px;">
                            <field name="outgoing_lines">
                                <tree editable="false" decoration-muted="1">
                                    <field name="description"/>
                                    <field name="item"/>
                                    <field name="item_serial_no"/>
                                    <field name="unit"/>
                                    <field name="type_of_material"/>
                                    <field name="date"/>
                                    <field name="quantity"/>
                                    <field name="notes"/>
                                </tree>
                            </field>
                        </page>

                        <!-- Incoming Lines Page -->
                        <page string="📥 Incoming Lines" style="background-color:#EBF5FB; padding:5px;">
                            <field name="incoming_lines">
                                <tree editable="false" decoration-muted="1">
                                    <field name="description"/>
                                    <field name="item"/>
                                    <field name="item_serial_no"/>
                                    <field name="unit"/>
                                    <field name="type_of_material"/>
                                    <field name="date"/>
                                    <field name="quantity"/>
                                    <field name="notes"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- LIST VIEW -->
    <record id="view_stock_transfer_report_list" model="ir.ui.view">
        <field name="name">stock.transfer.report.list</field>
        <field name="model">stock.transfer.report</field>
        <field name="arch" type="xml">
            <tree string="Stock Transfer Reports" decoration-muted="1">
                <field name="store"/>
                <field name="company"/>
            </tree>
        </field>
    </record>

    <!-- ACTION -->
    <record id="action_stock_transfer_report" model="ir.actions.act_window">
        <field name="name">Stock Transfer Reports</field>
        <field name="res_model">stock.transfer.report</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_stock_transfer_report_list"/>
    </record>

    <!-- MENU -->
    <menuitem id="menu_stock_transfer_report_root" name="Stock Transfer Reports"/>
    <menuitem id="menu_stock_transfer_report_main" name="Reports" parent="menu_stock_transfer_report_root" action="action_stock_transfer_report"/>
</odoo>
