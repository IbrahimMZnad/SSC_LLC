<odoo>
    <!-- 🔹 Root Menu -->
    <menuitem id="menu_ssc_attendance_root"
              name="Attendance"
              sequence="10"/>

    <!-- 🔹 Submenu -->
    <menuitem id="menu_ssc_attendance"
              name="Daily Attendance"
              parent="menu_ssc_attendance_root"
              action="action_ssc_attendance"
              sequence="20"/>

    <!-- 🔹 Action for Attendance -->
    <record id="action_ssc_attendance" model="ir.actions.act_window">
        <field name="name">Daily Attendance</field>
        <field name="res_model">ssc.attendance</field>
        <field name="view_mode">list,form</field>  <!-- ✅ عدلناها حسب طلبك -->
        <field name="target">current</field>
    </record>
</odoo>
